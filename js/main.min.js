window.addEventListener('DOMContentLoaded', () => {
  let a = () => {
    setTimeout(() => {
      document.querySelector('.find__title').classList.add('find__title-active'),
        setTimeout(() => {
          document.querySelector('.find__desc').classList.add('find__desc-active'), document.querySelector('.find__form').classList.add('find__form-active');
        }, 1e3);
    }, 1500);
  };
  a();
  let e = document.querySelector('.header__wrapper'),
    b = () => {
      document.querySelector('.header__sign-in').addEventListener('click', (a) => {
        a.preventDefault(), e.classList.add('header__wrapper-active');
      });
    };
  b();
  let c = () => {
    document.querySelector('.form__close').addEventListener('click', (a) => {
      a.preventDefault(), e.classList.remove('header__wrapper-active');
    });
  };
  c();
  let d = () => {
    let a = document.querySelector('.find__slider'),
      c = document.querySelector('.find__slider-big');
    function b() {
      return window.getComputedStyle(c).backgroundImage;
    }
    b(),
      a.addEventListener('click', (d) => {
        let a = d.target;
        if (a.classList.contains('find__slider-small_top') || a.classList.contains('find__slider-small_bottom')) {
          let e = window.getComputedStyle(a).backgroundImage;
          (a.style.backgroundImage = b()), (c.style.backgroundImage = e);
        }
      });
  };
  d(),
    (() => {
      let a = window.screen.height - (20 * window.screen.height) / 100,
        b = document.querySelectorAll('.service__icon'),
        c = document.querySelectorAll('.title'),
        d = document.querySelectorAll('.title-desc');
      window.addEventListener('scroll', () => {
        c.forEach((b) => {
          b.getBoundingClientRect().top <= a && b.classList.add('title-active');
        }),
          d.forEach((b) => {
            b.getBoundingClientRect().top <= a && b.classList.add('title-desc-active');
          }),
          b.forEach((b, c) => {
            b.getBoundingClientRect().top <= a &&
              setTimeout(() => {
                b.classList.add('show-service__icon');
              }, 500 + 500 * c);
          });
      });
    })(),
    new Swiper('.swiper', { loop: !0, effect: 'flip', flipEffect: { slideShadows: !0 }, navigation: { nextEl: '.swiper-button-next', prevEl: '.swiper-button-prev' } });
});
